<%= form_for([:admin, @item]) do |f| %>
<ul>
  <% categoy_all.each do |category| %>
      <li>
        <%= check_box_tag 'cateogory_ids[]', category.id -%>
        <%= category.name -%>
      </li>
  <% end %>
</ul>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>